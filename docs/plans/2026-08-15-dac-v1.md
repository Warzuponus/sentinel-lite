# Detection-as-Code v1 Implementation Plan

> **For Hermes:** Use local-qwen-coding. Planner (Grok) owns spec/tests; Qwen implements TASKS/011 only.

**Goal:** Add a YAML Detection-as-Code pack so each existing auth rule ships with a lintable spec and an analyst explanation — without changing detector logic.

**Architecture:** `rules/*.yml` documents are the source of truth for metadata + explanation. A small loader validates them. CLI `rules list|lint|explain` reads the pack. Hardcoded detectors stay as-is.

**Tech Stack:** Python 3.11+, existing pydantic + pyyaml. No new deps.

---

### Task 011: DAC rule pack + CLI

See `TASKS/011-dac-rules.md`. Tests are already in `tests/test_dac_rules.py` (must start RED).
